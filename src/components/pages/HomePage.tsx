import { HomeLayouts } from "../layouts/HomeLayouts";

type Props = {};

const HomePage = (props: Props) => {
  return (
    <>
      <div>HomePage</div>
      <HomeLayouts />
    </>
  );
};

export default HomePage;
